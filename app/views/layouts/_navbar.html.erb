  <nav class="bg-white/95 backdrop-blur-md border-b border-slate-200 fixed w-full z-30 top-0 start-0" data-controller="navbar">
  <div class="max-w-screen-2xl flex flex-wrap items-center justify-between mx-auto px-4 md:px-8 py-4">
    <div class="flex items-center">
      <a href="<%= root_path %>" class="md:hidden flex items-center mr-2">
        <% if @site_config.logo.attached? %>
          <%= image_tag @site_config.logo, class: "h-10 w-auto object-contain", alt: @site_config.name %>
        <% else %>
          <%= image_tag "yapai.webp", class: "h-10 w-auto object-contain", alt: "Yapai" %>
<% end %>
</a>
<a href="<%= root_path %>" class="hidden md:flex items-center space-x-3 rtl:space-x-reverse mr-4 md:mr-12 flex-shrink-0">
  <% if @site_config.logo.attached? %>
    <%= image_tag @site_config.logo, class: "h-12 sm:h-14 md:h-16 lg:h-20 w-auto max-w-[160px] sm:max-w-[200px] md:max-w-[280px] lg:max-w-[320px] object-contain transition-all duration-300", alt: @site_config.name %>
  <% else %>
    <%= image_tag "yapai.webp", class: "h-12 sm:h-14 md:h-16 lg:h-20 w-auto max-w-[160px] sm:max-w-[200px] md:max-w-[280px] lg:max-w-[320px] object-contain transition-all duration-300", alt: "Yapai" %>
  <% end %>
      </a>
    </div>
    <div class="flex md:order-2 space-x-3 items-center">

      <div class="hidden md:flex">
        <%= form_with url: products_path, method: :get, class: "relative group" do %>
          <div class="relative">
            <div class="absolute inset-y-0 left-0 flex items-center pl-3 pointer-events-none">
              <svg class="w-4 h-4 text-slate-400 group-focus-within:text-primary transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
              </svg>
            </div>
            <%= text_field_tag :q, params[:q], 
                placeholder: t('navbar.search_placeholder'), 
                class: "block w-full md:w-48 lg:w-64 pl-10 pr-4 py-2 text-sm text-slate-900 border border-slate-200 rounded-full bg-slate-50 focus:ring-primary focus:border-primary transition-all focus:bg-white focus:w-72" %>
          </div>
        <% end %>
      </div>

      <button data-action="click->navbar#toggle" type="button" class="inline-flex items-center p-2 w-10 h-10 justify-center text-sm text-slate-500 rounded-lg md:hidden hover:bg-slate-100 focus:outline-none focus:ring-2 focus:ring-slate-200" aria-controls="navbar-sticky" aria-expanded="false">
        <span class="sr-only">Open menu</span>
        <svg class="w-5 h-5" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 17 14">
          <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M1 1h15M1 7h15M1 13h15"/>
        </svg>
      </button>
    </div>

    <div class="items-center justify-between hidden w-full md:flex md:w-auto md:order-1 flex-grow" id="navbar-sticky" data-navbar-target="menu">
      <ul class="flex flex-col p-4 md:p-0 mt-4 font-medium border border-slate-100 rounded-lg bg-white md:space-x-4 lg:space-x-6 rtl:space-x-reverse md:flex-row md:mt-0 md:border-0 md:bg-transparent justify-center text-xs lg:text-sm">
        <li>
          <%= link_to t('navbar.home'), root_path, class: "block py-2 px-3 #{current_page?(root_path) ? 'text-primary font-bold border-b-2 border-primary' : 'text-slate-600 hover:text-primary'} transition-all duration-300 rounded-sm md:bg-transparent md:p-0" %>
        </li>

        <li class="relative group/products" data-navbar-target="rootDropdown">
          <div class="flex items-center">
            <div class="flex items-center py-2 px-3 text-slate-600 hover:text-primary transition-all duration-300 rounded-sm md:bg-transparent md:p-0 flex-grow cursor-pointer">
              <span><%= t('navbar.all_products') %></span>
              <svg class="hidden md:inline-block w-4 h-4 ml-1 transition-transform duration-200 group-hover/products:rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
              </svg>
            </div>
            <button type="button" data-action="click->navbar#toggleMobileRoot" class="md:hidden ml-auto text-slate-400 p-2 focus:outline-none hover:text-primary transition-colors">
              <svg class="w-4 h-4 transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path></svg>
            </button>
          </div>

          <!-- Products Dropdown -->
          <div class="hidden group-hover/products:block md:absolute md:left-0 md:top-full w-full md:w-64 z-50 pt-2 dropdown-container pointer-events-auto">
            <div class="bg-white border border-slate-200 rounded-2xl shadow-2xl overflow-visible backdrop-blur-xl bg-opacity-95 py-2 ring-1 ring-slate-900/5 md:animate-fade-in-up">
              <% [ 'apple', 'huawei', 'oppo', 'vivo', 'xiaomi', 'custom' ].each do |kind| %>
                <% categories_for_kind(kind).each do |category| %>
                  <%= render 'layouts/navbar_category', category: category, kind: kind, depth: 1 %>
                <% end %>
              <% end %>
            </div>
          </div>
        </li>
        <li>
          <%= link_to t('navbar.factory'), factory_path, class: "block py-2 px-3 #{current_page?(factory_path) ? 'text-primary font-bold border-b-2 border-primary' : 'text-slate-600 hover:text-primary'} transition-all duration-300 rounded-sm md:bg-transparent md:p-0" %>
        </li>
        <li>
          <%= link_to t('navbar.customize'), customize_path, class: "block py-2 px-3 #{current_page?(customize_path) ? 'text-primary font-bold border-b-2 border-primary' : 'text-slate-600 hover:text-primary'} transition-all duration-300 rounded-sm md:bg-transparent md:p-0" %>
        </li>


        <li class="relative group/blog" data-navbar-target="rootDropdown">
          <div class="flex items-center">
            <div class="flex items-center py-2 px-3 text-slate-600 hover:text-primary transition-all duration-300 rounded-sm md:bg-transparent md:p-0 flex-grow cursor-pointer">
              <span><%= t('navbar.blog') %></span>
              <svg class="hidden md:inline-block w-4 h-4 ml-1 transition-transform duration-200 group-hover/blog:rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
              </svg>
            </div>
            <button type="button" data-action="click->navbar#toggleMobileRoot" class="md:hidden ml-auto text-slate-400 p-2 focus:outline-none hover:text-primary transition-colors">
              <svg class="w-4 h-4 transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path></svg>
            </button>
          </div>

          <!-- Blog Dropdown -->
          <div class="hidden group-hover/blog:block md:absolute md:left-0 md:top-full w-full md:w-64 z-50 pt-2 dropdown-container pointer-events-auto">
            <div class="bg-white border border-slate-200 rounded-2xl shadow-2xl overflow-visible backdrop-blur-xl bg-opacity-95 py-2 ring-1 ring-slate-900/5 md:animate-fade-in-up">
              <%= link_to posts_path(category: 'enterprise_updates'), class: "block px-4 py-3 text-sm text-slate-600 hover:bg-primary hover:text-white transition-all truncate" do %>
                <%= t('navbar.enterprise_updates') %>
              <% end %>
              <%= link_to posts_path(category: 'industry_news'), class: "block px-4 py-3 text-sm text-slate-600 hover:bg-primary hover:text-white transition-all truncate" do %>
                <%= t('navbar.industry_news') %>
              <% end %>
            </div>
          </div>
        </li>
        <li>
          <%= link_to t('navbar.faqs', default: 'FAQs'), faqs_path, class: "block py-2 px-3 #{current_page?(faqs_path) ? 'text-primary font-bold border-b-2 border-primary' : 'text-slate-600 hover:text-primary'} transition-all duration-300 rounded-sm md:bg-transparent md:p-0" %>
        </li>
        <li>
          <%= link_to t('navbar.contact_us'), contact_path, class: "block py-2 px-3 #{current_page?(contact_path) ? 'text-primary font-bold border-b-2 border-primary' : 'text-slate-600 hover:text-primary'} transition-all duration-300 rounded-sm md:bg-transparent md:p-0" %>
        </li>
        <li class="md:hidden pt-4 border-t border-slate-100 mt-2">
          <%= form_with url: products_path, method: :get, class: "px-3" do %>
            <div class="relative">
              <div class="absolute inset-y-0 left-0 flex items-center pl-3 pointer-events-none">
                <svg class="w-4 h-4 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
                </svg>
              </div>
              <%= text_field_tag :q, params[:q], 
                  placeholder: t('navbar.search_placeholder'), 
                  class: "block w-full pl-10 pr-4 py-2 text-xs text-slate-900 border border-slate-200 rounded-full bg-slate-50 focus:ring-primary focus:border-primary transition-all" %>
            </div>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>
