<% content_for :title, "Yapai Battery Factory - Professional Mobile Phone Battery Manufacturer" %>
<% content_for :meta_description, "Yapai Battery Factory is a professional mobile phone battery manufacturer in Guangdong, China. OEM/ODM replacement batteries for iPhone, Huawei, Xiaomi, and 1,000+ brands." %>
<% content_for :meta_keywords, "mobile phone battery factory, smartphone replacement battery, Yapai Battery, iPhone battery wholesale, OEM battery manufacturer, China battery factory" %>

<%# ============================================================
    Hero Section — Rebuilt Responsive Slideshow
    ============================================================ %>
<section
  class="relative -mt-20 w-full overflow-hidden"
  style="height: 100svh; min-height: 600px;"
  data-controller="slideshow"
  aria-label="Hero Slideshow"
>

  <%# ── 1. Background Slides ── %>
  <div class="absolute inset-0 z-0">
    <% [1, 2, 3].each_with_index do |n, idx| %>
      <div
        class="absolute inset-0 transition-opacity duration-1000 ease-in-out opacity-0"
        data-slideshow-target="slide"
      >
        <%= image_tag "indexbanner#{n}.webp",
                      class: "w-full h-full object-cover object-center scale-105 transition-transform duration-[5000ms]",
                      alt: "",
                      role: "presentation",
                      loading: idx == 0 ? "eager" : "lazy" %>
      </div>
    <% end %>
  </div>

  <%# ── 2. Overlay Gradients (Improved Contrast) ── %>
  <div class="absolute inset-0 z-10 pointer-events-none">
    <%# Bottom dark gradient for text readability %>
    <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/20 to-transparent"></div>
    <%# Left side gradient for desktop text focus %>
    <div class="absolute inset-0 hidden lg:block bg-gradient-to-r from-black/40 via-transparent to-transparent"></div>
  </div>

  <%# ── 3. Content ── %>
  <div class="absolute inset-0 z-20 flex flex-col justify-end lg:justify-center">
    <div class="w-full max-w-screen-xl mx-auto px-6 sm:px-12 lg:px-20 pb-16 lg:pb-0">
      
      <% [1, 2, 3].each_with_index do |i, idx| %>
        <div
          class="absolute bottom-16 lg:bottom-auto lg:top-1/2 lg:-translate-y-1/2 
                 max-w-2xl opacity-0 translate-y-8 pointer-events-none
                 transition-all duration-1000 ease-out"
          data-slideshow-target="content"
        >
          <%# Tagline %>
          <div class="inline-flex items-center gap-2 mb-4">
            <span class="w-8 h-[2px] bg-primary rounded-full"></span>
            <span class="text-white/90 text-[10px] sm:text-xs font-black tracking-[0.3em] uppercase">
              <%= t("home.index.hero_slides.slide#{i}.title1") %>
            </span>
          </div>

          <%# Main Title %>
          <h1 class="font-black text-white leading-[1.1] mb-6 drop-shadow-2xl"
              style="font-size: clamp(2.25rem, 8vw, 5rem);">
            <%= t("home.index.hero_slides.slide#{i}.title1") %><br/>
            <span class="text-transparent bg-clip-text bg-gradient-to-r from-primary via-white to-primary bg-[length:200%_auto] animate-gradient-x">
              <%= t("home.index.hero_slides.slide#{i}.title2") %>
            </span>
          </h1>

          <%# Description %>
          <p class="text-white/70 text-sm sm:text-base lg:text-lg max-w-md mb-8 leading-relaxed font-medium">
            <%= t("home.index.hero_slides.slide#{i}.desc") %>
          </p>

          <%# Actions - Removed as per request %>
        </div>
      <% end %>
    </div>
  </div>

  <%# ── 4. Controls (Bottom Right) ── %>
  <div class="absolute bottom-8 right-6 lg:right-20 z-30 flex items-end gap-6">
    <%# Counter %>
    <div class="hidden sm:block text-white/50 font-mono text-sm tracking-tighter">
      <span class="text-white text-2xl font-black" data-slideshow-target="counter">01</span> / 03
    </div>

    <%# Indicators %>
    <div class="flex items-center gap-3">
      <% 3.times do |n| %>
        <button
          type="button"
          class="group relative h-10 w-1 flex items-center justify-center"
          data-action="click->slideshow#goTo"
          data-index="<%= n %>"
          aria-label="Go to slide <%= n + 1 %>"
        >
          <span class="block w-full h-full bg-white/20 rounded-full transition-all duration-500 group-hover:bg-white/40"
                data-slideshow-target="indicator"></span>
        </button>
      <% end %>
    </div>

    <%# Arrows %>
    <div class="flex gap-2">
      <button data-action="click->slideshow#prev" class="w-12 h-12 flex items-center justify-center rounded-full border border-white/20 text-white hover:bg-white/10 transition-all active:scale-90">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M15 19l-7-7 7-7"/></svg>
      </button>
      <button data-action="click->slideshow#next" class="w-12 h-12 flex items-center justify-center rounded-full border border-white/20 text-white hover:bg-white/10 transition-all active:scale-90">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M9 5l7 7-7 7"/></svg>
      </button>
    </div>
  </div>

</section>

<!-- ===== YAPAI PRODUCT MARQUEE ===== -->
<div class="bg-slate-900 py-8 overflow-hidden">
  <div class="text-center mb-6">
    <span class="text-2xl font-black text-slate-500 uppercase tracking-widest">Our Product Catalog</span>
  </div>
  <!-- Forward strip -->
  <div class="overflow-hidden mb-4">
    <div class="flex gap-5 w-max" style="animation: yapai-marquee 32s linear infinite;">
      <% 2.times do %>
        <% [["iphone.webp","iPhone Battery"],["vivo.webp","Vivo Battery"],["oppo.webp","oppo Battery"],["huawei.webp","Huawei Battery"],["xiaomi.webp","Xiaomi Battery"]].each do |img, label| %>
          <div class="relative flex-shrink-0 w-56 h-64 rounded-2xl overflow-hidden group cursor-pointer">
            <%= image_tag img, class: "w-full h-full object-cover group-hover:scale-110 transition-transform duration-700" %>
            <div class="absolute inset-0 bg-gradient-to-t from-black/70 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-end p-4">
              <span class="text-white font-bold text-sm tracking-wide"><%= label %></span>
            </div>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
  <!-- Reverse strip -->

</div>

<!-- ===== FEATURED PRODUCTS GRID ===== -->
<div class="bg-white py-20 overflow-hidden">
  <div class="max-w-screen-xl mx-auto px-6">
    <div class="text-center mb-14 reveal-up">
      <p class="text-primary font-semibold tracking-widest uppercase text-xs mb-3">Crafted with Precision</p>
      <h2 class="text-4xl md:text-5xl font-black text-slate-900">Featured Products</h2>
    </div>
    <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-3 gap-4">
      <% if @featured_categories.any? %>
        <% @featured_categories.each_with_index do |category, i| %>
          <%= link_to category_path(category), 
                      class: "reveal-product group relative rounded-3xl overflow-hidden shadow-md hover:shadow-2xl transition-all duration-500 hover:-translate-y-2 cursor-pointer", 
                      style: "transition-delay: #{i * 80}ms" do %>
            <div class="aspect-[3/4] overflow-hidden bg-white">
              <% if category.image.attached? %>
                <%= image_tag rails_storage_proxy_path(category.image.variant(:featured)), class: "w-full h-full object-cover group-hover:scale-105 transition-transform duration-700" %>
              <% else %>
                <div class="w-full h-full bg-slate-100 flex items-center justify-center text-slate-300 italic text-xs">No Image</div>
              <% end %>
            </div>
            <div class="absolute top-3 left-3">
              <span class="bg-primary text-white text-[10px] font-black uppercase tracking-widest px-2.5 py-1 rounded-full">Featured</span>
            </div>
            <div class="absolute bottom-0 left-0 right-0 bg-gradient-to-t from-black/90 via-black/50 to-transparent px-5 pb-6 pt-16">
              <p class="text-white font-black text-xl leading-tight drop-shadow-lg"><%= category.name %></p>
              <p class="text-white/75 text-xs font-bold mt-1 uppercase tracking-widest">Custom · OEM · ODM</p>
            </div>
          <% end %>
        <% end %>
      <% else %>
        <% [
          { img: "iphone.webp",    name: "iPhone Battery",           tag: "Best Seller", color: "primary",   fit: "cover" },
          { img: "oppo.webp",      name: "Android Battery",          tag: "Premium",     color: "secondary", fit: "cover" },
          { img: "huawei.webp",    name: "Huawei Battery",           tag: "High Capacity", color: "accent",    fit: "cover" },
          { img: "xiaomi.webp",    name: "Xiaomi Battery",           tag: "New Arrival", color: "primary",   fit: "cover" }
        ].each_with_index do |yapai, i| %>
          <div class="reveal-product group relative rounded-3xl overflow-hidden shadow-md hover:shadow-2xl transition-all duration-500 hover:-translate-y-2 cursor-pointer" style="transition-delay: <%= i * 80 %>ms">
            <div class="aspect-[3/4] overflow-hidden bg-white">
              <%= image_tag yapai[:img], class: "w-full h-full #{yapai[:fit] == 'contain' ? 'object-contain p-4' : 'object-cover'} group-hover:scale-105 transition-transform duration-700" %>
            </div>
            <div class="absolute top-3 left-3">
              <span class="bg-<%= yapai[:color] %> text-white text-[10px] font-black uppercase tracking-widest px-2.5 py-1 rounded-full"><%= yapai[:tag] %></span>
            </div>
            <div class="absolute bottom-0 left-0 right-0 bg-gradient-to-t from-black/90 via-black/50 to-transparent px-5 pb-6 pt-16">
              <p class="text-white font-black text-xl leading-tight drop-shadow-lg"><%= yapai[:name] %></p>
              <p class="text-white/75 text-xs font-bold mt-1 uppercase tracking-widest">Custom · OEM · ODM</p>
            </div>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
</div>

<!-- Who We Are & Categories Section -->
<div class="bg-white py-24 relative overflow-hidden">
  <div class="max-w-screen-xl mx-auto px-6 relative z-10">
    <div class="flex flex-col lg:flex-row gap-16 items-start">
      <div class="w-full reveal-up">
        <h2 class="text-4xl md:text-5xl font-black text-slate-900 mb-8 leading-tight text-center">
          <%= t('home.index.who_we_are_title') %>
        </h2>
        <p class="text-slate-500 text-lg leading-relaxed mb-8 text-center max-w-4xl mx-auto">
          <%= t('home.index.who_we_are_desc') %>
        </p>
        <div class="grid grid-cols-2 gap-4 max-w-2xl mx-auto">
          <div class="p-6 bg-slate-50 rounded-3xl border border-slate-100 shadow-sm text-center">
            <div class="text-3xl font-black text-primary mb-1">20</div>
            <div class="text-xs font-bold text-slate-400 uppercase tracking-widest"><%= t('home.index.years_history') %></div>
          </div>
          <div class="p-6 bg-slate-50 rounded-3xl border border-slate-100 shadow-sm text-center">
            <div class="text-3xl font-black text-secondary mb-1">100%</div>
            <div class="text-xs font-bold text-slate-400 uppercase tracking-widest"><%= t('home.index.oem_odm_capability') %></div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- Customization Process Section -->
<div class="bg-slate-50 py-24">
  <div class="max-w-screen-xl mx-auto px-6 text-center">
    <div class="mb-20 reveal-up">
      <h2 class="text-4xl md:text-5xl font-black text-slate-900 mb-4"><%= t('home.index.process_title') %></h2>
      <div class="h-1.5 w-24 bg-primary mx-auto rounded-full"></div>
    </div>
    
    <div class="grid grid-cols-1 md:grid-cols-4 gap-12 relative">
      <!-- Decorative Line -->
      <div class="hidden md:block absolute top-12 left-0 w-full h-0.5 bg-slate-200 z-0"></div>
      
      <% 4.times do |i| %>
        <div class="relative z-10 group reveal-up" style="transition-delay: <%= i * 100 %>ms">
          <div class="w-24 h-24 bg-white rounded-full border-4 border-slate-50 shadow-xl mx-auto flex items-center justify-center mb-8 group-hover:border-primary transition-colors duration-500">
            <span class="text-4xl font-black text-primary"><%= i + 1 %></span>
          </div>
          <h4 class="text-xl font-black text-slate-900 mb-4 uppercase"><%= t("home.index.process.step_#{i+1}_title") %></h4>
          <p class="text-slate-500 font-medium px-4">
            <%= t("home.index.process.step_#{i+1}_desc") %>
          </p>
        </div>
      <% end %>
    </div>
  </div>
</div>

<!-- Manufacturing Excellence Section (Strength) -->
<div class="bg-white py-24 relative overflow-hidden">
  <div class="max-w-screen-xl mx-auto px-6 relative z-10">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center">
      <div class="relative order-2 lg:order-1 reveal-left">
        <div class="grid grid-cols-2 gap-4">
          <div class="space-y-4 pt-12">
            <div class="aspect-square rounded-[2rem] overflow-hidden shadow-2xl hover:scale-105 transition-transform duration-500">
              <%= image_tag "factory1.jpg", class: "w-full h-full object-cover" %>
            </div>
            <div class="aspect-[4/3] rounded-[2rem] overflow-hidden shadow-2xl hover:scale-105 transition-transform duration-500">
              <%= image_tag "factory2.jpg", class: "w-full h-full object-cover" %>
            </div>
          </div>
          <div class="space-y-4">
            <div class="aspect-[3/4] rounded-[2rem] overflow-hidden shadow-2xl hover:scale-105 transition-transform duration-500">
              <%= image_tag "factory3.jpg", class: "w-full h-full object-cover" %>
            </div>
            <div class="aspect-square rounded-[2rem] overflow-hidden shadow-2xl hover:scale-105 transition-transform duration-500">
              <%= image_tag "factory4.jpg", class: "w-full h-full object-cover" %>
            </div>
          </div>
        </div>
        <div class="absolute -bottom-8 -right-8 bg-primary p-8 rounded-[2rem] shadow-2xl text-white animate-bounce hidden md:block">
          <div class="text-4xl font-black mb-1">20</div>
          <div class="text-xs font-bold uppercase tracking-widest">Years Exp.</div>
        </div>
      </div>
      
      <div class="order-1 lg:order-2 reveal-right">
        <h2 class="text-4xl md:text-5xl font-black text-slate-900 mb-8 leading-tight">
          <%= t('home.index.strength_title') %>
        </h2>
        
        <div class="space-y-8">
          <% ['no_moq', 'flexible', 'easy_process', 'direct_factory'].each_with_index do |key, idx| %>
            <% color = ['primary', 'secondary', 'accent', 'primary'][idx] %>
            <div class="flex items-start space-x-6 group">
              <div class="w-14 h-14 bg-<%= color %>/10 rounded-2xl flex-shrink-0 flex items-center justify-center text-<%= color %> group-hover:bg-<%= color %> group-hover:text-white transition-all duration-300">
                <svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path></svg>
              </div>
              <div>
                <h4 class="text-xl font-black text-slate-900 mb-2 uppercase tracking-tight"><%= t("home.index.strengths.#{key}.title") %></h4>
                <p class="text-slate-500 font-medium leading-relaxed">
                  <%= t("home.index.strengths.#{key}.desc") %>
                </p>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>


<!-- Final CTA Section -->
<div class="bg-white py-24">
  <div class="max-w-screen-xl mx-auto px-6">
    <div class="relative bg-primary p-12 md:p-20 rounded-[3rem] overflow-hidden shadow-3xl text-center md:text-left flex flex-col md:flex-row items-center justify-between gap-12">
      <div class="relative z-10 max-w-2xl">
        <h2 class="text-4xl md:text-6xl font-black text-white mb-6 leading-tight">
          <%= t('home.index.final_cta_title') %>
        </h2>
        <p class="text-white/80 text-xl font-medium">
          <%= t('home.index.final_cta_desc') %>
        </p>
      </div>
      
      <div class="relative z-10 flex-shrink-0">
        <%= link_to contact_path, class: "px-12 py-6 bg-white hover:bg-slate-50 text-slate-900 rounded-2xl font-black text-lg tracking-widest transition-all shadow-2xl flex items-center group" do %>
          <%= t('home.index.get_free_quote') %>
          <svg class="w-6 h-6 ml-4 group-hover:translate-x-2 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M17 8l4 4m0 0l-4 4m4-4H3"></path></svg>
        <% end %>
      </div>
    </div>
  </div>
</div>

<style>
  @keyframes yapai-marquee {
    from { transform: translateX(0); }
    to   { transform: translateX(-50%); }
  }
  @keyframes yapai-marquee-rev {
    from { transform: translateX(-50%); }
    to   { transform: translateX(0); }
  }

  .reveal-up, .reveal-left, .reveal-right, .reveal-product {
    opacity: 0;
    transform: translateY(28px);
    transition: opacity 0.65s ease, transform 0.65s ease;
  }
  .reveal-left  { transform: translateX(-36px); }
  .reveal-right { transform: translateX(36px); }

  .is-visible {
    opacity: 1 !important;
    transform: none !important;
  }
</style>

<script>
  function initHomeAnimations() {
    const els = document.querySelectorAll(".reveal-up, .reveal-left, .reveal-right, .reveal-product");
    const obs = new IntersectionObserver((entries) => {
      entries.forEach((e) => {
        if (e.isIntersecting) {
          e.target.classList.add("is-visible");
          obs.unobserve(e.target);
        }
      });
    }, { threshold: 0.05 });

    els.forEach((el) => {
      const rect = el.getBoundingClientRect();
      if (rect.top < window.innerHeight && rect.bottom > 0) {
        el.classList.add("is-visible");
      } else {
        obs.observe(el);
      }
    });
  }

  document.addEventListener("DOMContentLoaded", initHomeAnimations);
  document.addEventListener("turbo:load", initHomeAnimations);
</script>
